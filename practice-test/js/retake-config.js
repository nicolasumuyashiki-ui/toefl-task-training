/**
 * retake-config.js
 *
 * One-time Listening retake (apology remedy) eligibility list — shared by
 * results.html and test-select.html so the two pages can't drift apart.
 *
 * The mapping from these account IDs to real customer names lives ONLY in
 * docs/internal-accounts.md (not customer-facing). Do not add real names
 * to this file or to any comment near where it's used — this file ships
 * to the browser and its source is publicly viewable.
 */
window.PT_RETAKE_IDS = ['Kirari', 'Kotaro'];
